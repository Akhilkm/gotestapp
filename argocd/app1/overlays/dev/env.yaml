apiVersion: apps/v1
kind: Deployment
metadata:
  name: gotestapp
spec:
  selector:
    matchLabels:
      app: gotestapp
  template:
    metadata:
    spec:
      containers:
      - name: gotestapp
        env:
        - name: message
          value: "Hello from app1 dev environment"
